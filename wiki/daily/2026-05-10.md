# 2026-05-10

## Brand pass 2 + scrub playback shipped to main (PR #40 → `1300c3e`)

Day-long iteration on the move-list panel + scrub-playback feature. Started with brand pass 2 still on `feat/figma-brand-pass-2`, layered Polish B's move-list stepper on top, then iterated through user feedback into a full GSAP-timeline-driven scrub playback with editorial controls.

Squash-merged dev → main via PR #40 after the standard add/add reconciliation merge (see [[lesson-dev-main-merge-after-squash]]). Stable production alias updated.

### What landed

- **Move-list polish**
  - CSS-driven entry tween — replaces GSAP useGSAP-after-hydration approach. Fires from first SSR paint, kills the ~2s perceptible delay user reported.
  - Quadratic gap-decay stagger curve: gap(0)=30ms → gap(N-2)=5ms across N cells. Closed-form running sum via the standard sum-of-squares identity, no per-render loop.
  - `baseline` state locks at first paint via useEffect; cells with `cellPos < baseline` keep their original delay forever, fresh arrivals use a different keyframe via `.move-cell-fresh`.
  - Black-cell placeholder dropped during white's turn (no more pre-render of black square before they've moved).
  - Optimistic move-list entry appears at piece-place time (commitOptimisticAndSubmit append), not server-confirm. Dedup-by-ply absorbs the canonical version when it lands.

- **Scrub playback**
  - GSAP timeline walks the chessboard through every intermediate FEN on click. Per-move duration: `clamp(20, 200, 2000/N)` ms; bounded ~2s reveal regardless of game length.
  - `displayFen` overridden by `scrubPlaybackFen` during playback so `viewedPly` updates can lag the board.
  - `viewedPly` walks alongside the timeline so move-list highlight tracks the painted board.
  - Drag disabled (`scrubAnimDuration === null` gate) during playback.
  - Re-click mid-playback: kills tl, takes `currentScrubPlyRef` as new start, builds fresh tl. Auto-snap (livePly bump) preempts: kills tl, snaps to live.

- **Header controls**
  - 4 step buttons: ArrowLeftToLine | ArrowLeft | ArrowRight | ArrowRightToLine (lucide).
  - Play button on header row (top-right of "Move list" title): solid Play icon (`fill="currentColor"`), 1s/move pacing decoupled from piece tween (200ms), `bg-accent text-accent-foreground` when actively playing.
  - `handleScrub(target, opts: { paceMs?, isPlay? })` — opts upgrade from positional `paceMs`. Step buttons + cell clicks omit opts (curve, isPlaying=false). Play passes `{ paceMs: 1000, isPlay: true }`.

- **Piece easing unified**
  - Replaced react-chessboard default `ease` curve (visible asymmetry between fast and slow tweens chained) with `cubic-bezier(0.4, 0, 0.2, 1)` Material standard / chess.com / chessground convention.
  - Applied via `[data-piece] { transition-timing-function: ... !important }` globally so it wins over react-chessboard's inline shorthand without clobbering duration.
  - Default live tween 200ms (was 300ms react-chessboard internal).

- **Captured strip**
  - First icon `marginLeft: -8` to align with player name baseline (SVG transparent margin would otherwise inset visually).
  - Successor overlap `-12px` (was -8) — denser ribbon that still reads piece-by-piece.

### Cross-cutting wins

- **Promoted to global memory** (`~/.claude/memory/tools/`):
  - `git-squash-divergence.md` — confirmed at brand-pass-2 ship that the same `--ours` resolution holds for every dev → main promotion.
  - `postgres-function-overloading.md` — drop old signature before `create or replace`, otherwise both forms coexist.
  - `vercel-cron-hobby-limits.md` — 2 jobs / hourly minimum / Bearer-secret auth.

### Open

- Polish A (draw-by-agreement) still queued.
- Polish C (mobile / touch) still queued.
- Per-move clock display in MoveList — deferred from Polish B v1.
- Privatize v1 repo — awaits explicit user go.
- Email confirmation re-enable — awaits public-release ramp.

### Notes

- Vercel preview rebuild times ~3-4 min on this repo; CI lint-and-test stable.
- Re-confirmed [[lesson-dev-main-merge-after-squash]] resolution path holds for the brand-pass-2 ship: 5 conflicted files (`GameClient.tsx`, `TerminalBanner.tsx`, `globals.css`, `wordmark.tsx`, `narrative-chess-v2.md`), all resolved with `git checkout --ours` since dev was the superset post-frontend-pass-1.

## Figma "09 - Game" sync (post-ship)

After the PR #40 squash, swept through Figma to bring the `09 - Game` page (Light + Dark frames) up to current code. Removed the obsolete timer chips above + below the board and the old PGN / Result blocks; rebuilt the layout to mirror `GameClient.tsx`:

- **InGameBanner** above the board (854 × 87, inverted dark panel: `bg-foreground` + `text-background` + signal-red `> TURN` eyebrow + Fraunces title + mono subtitle).
- **Player Pills row** below the board (640 × 72, three cells: white pill + center turn pill + black pill — pills hardcoded white/black per code's `bg-white text-black` / `bg-black text-white` literals; borders + turn-pill bg still bound to `Palette/rule-soft`).
- **MoveList sidebar** right of the board (200 × 640, 24 numbered move pairs with white/black side-tints, active black move at row 23 `Rxb6` painted in `Palette/oxblood`).

Created two missing tokens in the Color Tokens collection so all theme-tracking paints could bind:

- `Palette/rule-soft` — Light `#d6ccc0` / Dark `#433b35`
- `Palette/bg-soft` — Light `#ebdfcf` / Dark `#433b35`

### Two Figma plugin gotchas hit (promoted to global memory)

- Bound paint's `opacity` field stores correctly but **renders ignored** — must put alpha at the node level on a child shape that holds the bound fill. → `~/.claude/memory/tools/figma-bound-paint-alpha.md`
- Manual `x` / `y` on auto-layout children is a no-op — need `parent.layoutMode = 'NONE'` AND `child.layoutPositioning = 'ABSOLUTE'` (the latter doesn't auto-clear on disable). → `~/.claude/memory/tools/figma-autolayout-positioning.md`

### Asset note

- [[taylor-piece-set]] — captured the fact that the Taylor pieces are hand-drawn by the project owner, not third-party assets. Refresh path from Figma `04 - Chess Pieces` page documented for future syncs.

## Polish A + Polish C shipped to main (PR #43 → `697c963`)

Long afternoon iterating on game-page layout (Polish C, mobile + desktop) and then shipping both Polish A (draw-by-agreement) and Polish C together via dev → main. Move-list went through ~16 commits of column-major / multi-col / snap-to-content / single-col reversion before landing on the chess.com / lichess convention: one tall column, scroll to follow play.

### Polish C — mobile + desktop layout polish (PR #41 → `a153788`)

- **Game-page composition**
  - Outer container `max-w-[768px] mx-auto` — sums to exactly board (576) + gap (12) + list (180); board never gives up width, list never grows past 180, cluster centers in main
  - Left column `basis-[576px] shrink-0` so the board is locked at 576px regardless of viewport
  - List wrapper `relative w-[180px] shrink-0` + inner `absolute inset-0 overflow-y-auto`. The absolute trick is what kills the spanning-grid row-inflation bug (long lists were stretching banner + pills cells before). See `~/.claude/memory/tools/css-flex-grid-sizing-traps.md` — promoted today.
  - Move-list panel: header (title + Play + step buttons) non-scrolling, only moves grid scrolls
  - `scrollIntoView({ block: nearest, inline: nearest })` scoped to desktop via `[data-desktop-list]` attribute so mobile cells don't end up scrolling the window
  - MoveCell drops `transition-colors` — instant on/off highlight reads cleanly during scrub
  - Step buttons keep `h-6` editorial micro-button visual, gain transparent `::before` overlay with `-m-2.5` so the actual hit zone reaches ~44px (WCAG 2.5.5)

- **Captured strip rebuilt with container queries**
  - Container queries on the player pill drive icon size (20 → 24px) + overlap (-12 → -4) by pill width
  - `h-[24px]` locked so pill height stays stable across viewports (was bouncing when icons scaled)
  - Right-edge mask fade catches residual overflow softly instead of hard-clipping mid-icon
  - Pattern promoted to `~/.claude/memory/tools/tailwind-v4-container-queries.md`

- **Site header**
  - 820px breakpoint for stacked → single-line wordmark (was lg=1024)
  - Fluid wordmark sizing via `clamp()`
  - Progressive nav hide: theme toggle below 480, Current below 380
  - Server-resolved currentGameId (last-played in-progress game, fallback to most recent in-progress where user is participant)
  - Baseline-aligned nav items, themed (rounded) theme toggle
  - "Current game" → "Current"

- **Mobile move list**
  - Auto-fill grid wraps multi-pair units per row, keeps white/black grouped (no orphan black after white)
  - Height grows to fit content — no separate scroll

- **Misc**
  - StageOverlay dropped 'Two players sit...' deck above CTA
  - Turn pill locked at `sm:w-[140px]` + collapses below sm to keep player pills stable

### Polish A — draw by agreement (PR #42 → `e801e36`)

- Migration `20260510120000_draw_by_agreement.sql` applied to hosted Supabase. FK typo (`profiles(id)` → `profiles(user_id)`) caught by db push and fixed before commit
- Schema: `games.draw_offered_by uuid` column + termination_reason CHECK extended for `draw_agreement` + `timeout` (was a possible reason but missing from the enum)
- RPCs (security definer, participant-guarded): offer_draw / withdraw_draw / accept_draw / decline_draw
- make_move RPC auto-clears `draw_offered_by` on every move (chess convention: moving implicitly declines / withdraws)
- Server actions matching existing resign/abortGame discriminated-union pattern
- GameActions UI renders three contextual states above resign/abort: Accept/Decline banner (opponent offered), Withdraw banner (I offered), Offer Draw button (no offer)
- TerminalBanner adds 'By agreement.' + 'By timeout.' subtitles
- Realtime: draw_offered_by added to GameStatusUpdateEventSchema

### Ship — dev → main (PR #43 → `697c963`)

- Squash-merge dev → main hit the standard add/add divergence (5 files: CapturedStrip, GameClient, MoveCell, MoveList, page.tsx + wordmark + project-state wiki). Resolved via `git checkout --ours` per [[lesson-dev-main-merge-after-squash]] — same `--ours dev = superset` pattern as every previous ship
- CI re-ran on the merge commit (4 min), green, squash-merge landed
- Both feat branches deleted (local + remote): `feat/polish-c-mobile-quick-wins`, `feat/draw-by-agreement`

### Cross-cutting wins (promoted to global memory)

- `~/.claude/memory/tools/tailwind-v4-container-queries.md` — `@container/name` parent + `@[width]/name:utility` child + arbitrary CSS vars per stop. Used by CapturedStrip to scale icons by pill width
- `~/.claude/memory/tools/css-flex-grid-sizing-traps.md` — three related traps: grid spanning items inflate rows, flex items-stretch grows shorter siblings, scrollIntoView wrong-container / single-axis. All three hit and fixed in sequence during the Polish C iteration. Captured the absolute-inside-relative-flex-item pattern that's the actual fix

### Open

- Per-move clock display in MoveList — still deferred
- Privatize v1 repo — still awaits explicit user go
- Email confirmation re-enable — awaits public-release ramp
- M2 narrative layer brainstorm

### Notes

- Iteration count on the move list was the highest of any single feature this project: row-major → column-major (3-stop) → snap-to-content cols → single-col revert. The path was real design exploration, not over-engineering — the column-major versions taught us what didn't work (reading flow ambiguity, threshold layout shift), which is why the final single-col + scroll convention reads as confidently chosen rather than path-of-least-resistance
- Husky pre-commit succeeded on every push today; the Windows-MINGW shebang fix is stable
- Vercel preview builds ~3-4 min still; CI lint-and-test ~3-4 min
