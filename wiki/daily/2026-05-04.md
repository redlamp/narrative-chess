# 2026-05-04

## Ship — M1.5 to production

PR #18 squash-merged to `main` at `681f809`. Bundle:

- Phase 7 (games directory + observer count, PR #13)
- Terminal banner Back-to-games button (PR #15)
- Site-wide header nav (PR #16, e2e #17)
- Vercel default-previews restore (PR #14)
- Phase 8 — landing page + 3D hero + auth dialog (PR #19)

Stable production alias: https://narrative-chess.vercel.app
M1.5 deployment: https://narrative-chess-ifumm993n-taylor-8571s-projects.vercel.app

Reconciliation merge `b14e2d6` healed the post-M1 squash-divergence on dev
(see [[lesson-dev-main-merge-after-squash]] — pattern confirmed twice
now).

## Phase 8 polish iterations (pre-ship)

3D hero went through six rounds of feedback:

1. Width fit — added `useThree().viewport`-driven group scale clamped to
   `min(1, viewport.width / 9.5)`.
2. Stem-base connection on rook — top cap dropped Y=0.85 → 0.79,
   battlements 0.97 → 0.93. Body Y ranges lowered so trunks overlap base
   by 0.05.
3. Common ground baseline — anchored "Chess" text and all pieces at
   `PIECE_Y = -1.7`; "Narrative" stacks at `PIECE_Y + 1.2`.
4. Arc layout — pawn / bishop / rook on left at progressively deeper Z;
   king / queen on right mirror.
5. Centered title — each `<Text3D>` in its own `<Center disableY>` so
   "Chess" and "Narrative" align horizontally despite different widths.
6. Bevel — `bevelEnabled, bevelSize=0.025, bevelThickness=0.04, bevelSegments=4`.

Plus content above mid-screen (`SCENE_Y = 1.5`), camera lifted for ~14°
downtilt (`CAMERA_Y = 1.5`, lookAt origin), device-orientation parallax
on mobile.

## WebGL Context Lost — solved (twice)

Round 1: `reactStrictMode: false` in `next.config.ts` (Strict-Mode
double-mount disposes the renderer GL context).

Round 2 (today): hero still flashed-then-blanked even with strict mode
off. Browser console showed `THREE.WebGLRenderer: Context Lost.` after
mount but no auto-restore. Cause: Turbopack HMR re-instantiates the
module + spawns a new `<Canvas>` whose createRoot rebinds GL state. The
browser won't attempt restoration unless the `webglcontextlost` listener
calls `e.preventDefault()`.

Fix: `<Canvas onCreated={attachContextRecovery}>` adds
`webglcontextlost` (preventDefault) + `webglcontextrestored`
(invalidate). Captured both layers in
[[lesson-webgl-strict-mode-context-loss]].

## Process — `--no-ff` for feat → dev

User noticed git history doesn't show branch fan-out anymore — every
feat-branch had been squashed into dev, collapsing the visualization.
Switched policy:

- feat → dev: `gh pr merge --merge` (no-fast-forward, preserves merge
  arc + commits)
- dev → main: stays `--squash` (linear history required by main
  protection)
- never `--rebase` (rewrites SHAs of others' commits)

CLAUDE.md updated at `0206d50`. Forward-only — existing main history
stays squashed; the next feat-branch landing on dev will be the first to
show the branch arc in the timeline.

## Memory + wiki organize pass

Routine end-of-ship pass triggered by user "organize memories":

- `user_role.md` — refreshed Current ship status (M1.5 SHIPPED), Next
  pickup (drop closed Phase 7/8 items, add AuthDialog onSuccess + theme
  toggle UI follow-ups), Branch state (dev = main + one docs commit).
  Sticky gotchas: added R3F + Strict Mode + R3F v9 type changes + merge
  strategy switch.
- Project memory `general.md` — added M1.5 ship facts, switched section
  title from "M1 ship facts" to "Ship facts".
- Wiki project page — added M1.5 row to phases-shipped table, replaced
  "Staged on dev" content (was Phase 7/8/banner) with current docs-only
  commit, refreshed Open threads (drop closed items, add Phase 8
  follow-ups), added new lesson link.
- Wiki `mocs/architecture.md` — indexed new lesson.
- This daily note.

## Next pickup

1. Verify the Hero3D context-recovery layer fixes the disappearance
   locally (user testing).
2. Decide M1.5+ vs M2 direction.
3. Step N — privatize v1 (always wait for explicit go).

## Afternoon — A/B/C ship + polish PRs

User picked all five (A/B/C/D/E "go for it"). A/B/C are small + clear,
D + E need brainstorms. Dispatched A/B/C as parallel subagents on
isolated worktrees. All three returned green, opened own PRs.

- **PR #20** (Hero3D context recovery) — merged `362ccac`. First
  `--no-ff` ride; branch arc visible in dev's graph. Fix layered on
  top of `reactStrictMode: false`: `<Canvas onCreated>` attaches
  `webglcontextlost` (preventDefault) + `webglcontextrestored`
  (invalidate). User confirmed working on localhost + Vercel.
- **PR #23** (AuthDialog onSuccess) — merged `47bd772`. Server
  actions split into redirecting + no-redirect variants;
  `LoginForm`/`SignUpForm` accept action prop; AuthDialog uses
  no-redirect variant + closes dialog + client-routes via
  `router.push("/games")`. New e2e `auth-dialog.spec.ts` covers it.
  All 3 e2e specs green locally (5.2s).
- **PR #21** (Theme toggle UI) — merged `2f31936`. `next-themes`
  ThemeProvider in root layout, Sun/Moon shadcn ghost icon button
  in SiteHeader. Tailwind `dark:` variants drive the icon swap so
  no SSR theme read needed (avoids hydration mismatch without the
  `mounted` flag pattern).
- **PR #22** (Fool's mate smoke button) — merged `f3df4f8`.
  Dev-only per-side button; click sets `armedRef`, `useEffect`
  keyed on `[ply]` dispatches the player's next move. User
  noticed button missing on Vercel preview — root cause:
  Vercel sets `NODE_ENV=production` for both preview AND
  production builds, so the original `NODE_ENV !== "production"`
  gate hid it. Fixed by surfacing `VERCEL_ENV` to the client via
  `next.config.ts` → `env.NEXT_PUBLIC_VERCEL_ENV`, gate now
  `NEXT_PUBLIC_VERCEL_ENV !== "production"`. DCE preserved
  because Next inlines NEXT_PUBLIC_* at build time.

## Both-sides single-click smoke — declined

User asked about a single button driving both white + black moves.
Surfaced 3 options:
1. service-role server action bypassing `make_move` RPC auth (god-mode
   write path; needs strict `VERCEL_ENV` gating; high blast radius if
   gate ever leaks)
2. solo-mode game type (forks data model, two code paths from now on)
3. canned animation/replay (no DB writes, but doesn't smoke the
   real game-end flow which is the whole point)

User chose status quo: stay with current per-side design. Two-browser
smoke is the workflow.

## Husky `--no-verify` issue

Two of the three subagents (B + C) used `--no-verify` after manually
running `bunx lint-staged` to confirm checks pass. Per CLAUDE.md
that's not OK without explicit ask. Root cause: `.husky/pre-commit`
has no shebang + CRLF endings; main worktree works because
`core.hooksPath = .husky/_` (husky 9 dispatcher) handles dispatch,
but fresh worktrees on MINGW try to exec the user-level hook
directly → `Exec format error`. One-line chore PR (`#!/bin/sh` +
LF) would fix it permanently. Captured in user_role sticky gotchas
+ wiki Open threads.

## Memory + wiki organize (end-of-day)

- `user_role.md` — Current ship status now lists all 4 PRs; Next
  pickup re-prioritized (top: dev → main promotion); Sticky gotchas
  expanded with NODE_ENV vs VERCEL_ENV + husky pre-commit issue.
- `.claude/memory/general.md` — Ship facts table extended.
- `wiki/projects/narrative-chess-v2.md` — Staged-on-dev table + Open
  threads refreshed; closed AuthDialog/Theme toggle/fool's mate
  entries.
- This daily note appended.

## Wrap-up — Husky chore + M1.5+ ship

User asked to do "1 and 2 from next session before I wrap up" —
husky chore + dev → main promotion. Both done.

- **PR #24** — Husky pre-commit shebang + LF eol + new
  `.gitattributes` pinning `.husky/* eol=lf`. Merged `dd42c4e`.
  Verified: pre-commit runs cleanly, no `--no-verify` needed.
- **PR #25** — `dev` → `main` squash. Bundled PRs #20/#21/#22/#23/#24
  + the docs/policy commits into a single squash commit on `main`
  (`67243d5`). First squash hit conflict (squash-divergence per
  lesson); reconciled with `git merge origin/main` + `--ours` for
  all 13 conflicted files (dev is the superset). Reconciliation
  merge `a1181d3` then re-merged origin/main back into dev as
  `9f6fa08` for content-equality.

M1.5+ deployed to production at
`https://narrative-chess-4a3g8vzog-taylor-8571s-projects.vercel.app`.
Stable alias `https://narrative-chess.vercel.app` now serves the
M1.5+ build.

`dev` and `main` are now content-equal at M1.5+. Next session starts
clean.
