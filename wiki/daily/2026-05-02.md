# 2026-05-02

## Done

- Set up structured memory system
  - Global `~/.claude/memory/` (cross-project): `memory.md` index, `general.md` conventions, empty `tools/` + `domain/`
  - Project-local `.claude/memory/` in workspace: same structure scoped to narrative-chess-v2
  - Project session-storage MEMORY.md (`~/.claude/projects/...`) initialised + Global Memory pointer added
- Built `~/.claude/CLAUDE.md` from scratch with 5 sections: Memory Management, Global Memory, Global Memory Reference Rule, Repo Memory Auto-Init, Domain Knowledge Lifecycle
- Built project root `CLAUDE.md` mirroring the same memory rules at project scope
- Added auto-memory-injection hook
  - `~/.claude/hooks/pre-tool-memory.py` reads stdin payload, dedups via `session_id`, injects session + project + global memory once per Claude Code session via PreToolUse hook
  - `~/.claude/hooks/pre-tool-memory.sh` thin bash wrapper
  - Registered in `~/.claude/settings.json` under `hooks.PreToolUse`
  - Adapted for Windows: `python` (not `python3`), flag file in `~/.claude/cache/` (not `/tmp`), reads both session-storage and workspace-local memory paths
- Captured email split as durable info — Claude account `twright@launchpad.build`, Git author `taylor@redlamp.org`. Saved to `~/.claude/memory/general.md` (cross-project) and project `user_role.md`
- Set up Obsidian wiki — see [[index]]

## Decisions

- See [[decision-three-way-info-split]] (info architecture)
- See [[decision-memory-injection-via-hook]] (auto-context strategy)

## Open questions

- When repo gets `git init`, set local `user.email` to `taylor@redlamp.org` (not Anthropic email)
- Whether to migrate `user_role.md` content from session-storage memory into workspace `.claude/memory/` (currently lives only in session-storage)
- Whether to add a sweep job to clean up old `~/.claude/cache/memory-loaded-*` flag files

## Notes created today

- [[decision-three-way-info-split]]
- [[decision-memory-injection-via-hook]]
- [[narrative-chess-v2]] (project starter page)

## V2 plan audit + design session (afternoon)

Audited `docs/V2_PLAN.md` against v1 GitHub state, v1 Supabase project, current Next.js / Vercel best practices.

### v1 Supabase audit (via MCP)

- 33 migrations, narrative + multiplayer schemas tangled
- `cities` (1), `city_editions` (1), `city_versions` (4 × ~33 KB jsonb) — narrative content with reuse value
- `game_threads` (3), `game_participants` (6), `game_moves` (**0**) — multiplayer schema with **zero played moves ever**
- 2 auth.users
- Confirms v1 PRD claim that multiplayer never worked

### Decisions captured

- [[decision-stack-nextjs-16]] — bump from Next.js 15 to 16.2
- [[decision-fresh-supabase-project]] — new SB project; export v1 narrative JSON first; pause v1 project
- [[decision-rpc-move-append]] — Postgres RPC with `expected_ply` optimistic concurrency
- [[decision-supabase-local-dev]] — hosted-first; Docker deferred with documented trigger conditions
- [[decision-vercel-branch-filter]] — `main` + `dev` auto-deploy only
- [[decision-auth-email-password]] — email + password for M1; OAuth M2+

### Spec drafted

- `docs/superpowers/specs/2026-05-02-v2-foundation-design.md` — supersedes V2_PLAN.md
- Awaiting user review before transition to writing-plans (implementation plan)

### Open from morning, still open

- Repo `git init` happened in scaffolding; `user.email` not yet set per [[narrative-chess-v2]]
- Migrate `user_role.md` from session-storage to workspace `.claude/memory/` — still TBD
- `~/.claude/cache/memory-loaded-*` flag-file sweep — still TBD

### New open from afternoon

- v1 repo `redlamp/narrative-chess-v1`: 1 commit unpushed (`58c6eca feat(ui): hide sign-in button + non-Historic Games tabs`). Push before any v2 GH-side action.
- v1 repo still public. Privatize deferred until M1 ships (decision in spec §4).
- v1 Supabase project: pause after content export (Step G in spec).

### Notes created (afternoon)

- [[decision-stack-nextjs-16]]
- [[decision-fresh-supabase-project]]
- [[decision-rpc-move-append]]
- [[decision-supabase-local-dev]]
- [[decision-vercel-branch-filter]]
- [[decision-auth-email-password]]

## Evening — autonomous work session

Effort: xhigh. Caveman mode active.

User stepped away. Authorized list: write Phase 2 plan, write Phase 3 plan, audit spec, pre-stage Phase 1 conventions on a non-merged branch, update daily log. Strategic question answered (local-only first vs cloud-from-start): chose to bake mock-first board UI into Phase 4 plan rather than build a fully local single-player throwaway.

### Done

- **GitHub repo bootstrap (Phase 1 Tasks 1+2)** — pushed v1's last commit `58c6eca`; created `redlamp/narrative-chess` (public); pushed `main` + `dev`. Verified.
- **Memory reorganization** ("reorganize memory" command). Three-way memory split now consistent: identity + communication prefs in global `~/.claude/memory/general.md`; project-specific state in session-storage `user_role.md`; conventions + workspace pointers in project-local `.claude/memory/general.md`. Email duplication removed from session-storage. Indexes updated.
- **Phase 2 plan** at `docs/superpowers/plans/2026-05-02-v2-phase-2-supabase-foundation.md` (1292 lines, 21 tasks across two branches). Covers spec Steps G + H: export v1 narrative content, fresh Supabase project, profiles+trigger migration, `@supabase/ssr` clients (browser/server/middleware), Zod env validation, sign-up + login + logout pages, auth-aware landing, redirect URL allow list, smoke test on live `dev` preview.
- **Phase 3 plan** at `docs/superpowers/plans/2026-05-02-v2-phase-3-schema-rls-realtime-gate.md` (743 lines, 10 tasks). Covers spec Step I — the critical gate phase: games + game_moves migration with RLS + Realtime publication, diagnostic UI at `/diagnostics/realtime` (kept in-repo for future regression checks), manual two-browser positive + negative gate procedure with explicit pass/fail criteria, wiki note for re-running the procedure, Playwright scaffold (fixme until Phase 4 auth helper).
- **Spec audit fixes**:
  - Vercel `dev` alias renamed `dev.narrative-chess.vercel.app` → `dev-narrative-chess.vercel.app` (free tier doesn't allow custom subdomains on `.vercel.app`). Same fix applied to [[decision-vercel-branch-filter]].
  - RLS clarification: explicit "NO INSERT/UPDATE/DELETE policies on games/game_moves; writes only via SECURITY DEFINER RPCs" — locked-by-default posture documented.
  - Migration ordering note: `init_profiles` (Phase 2) must precede `init_games` (Phase 3) due to FK dependency.
  - Programmatic auth helper: noted Phase 4 will add Playwright fixture; until then RLS+Realtime gate is manual.
- **Phase 1 conventions pre-staged** on branch `feat/conventions-stage` (NOT merged): `vercel.json`, `.github/workflows/ci.yml`, `.github/pull_request_template.md`, `AGENTS.md`, `.mcp.json` (without project_ref — set after Phase 2 creates v2 project), and AI rails appended to `CLAUDE.md`. Branch pushed for visibility. README.md not pre-staged because Next.js scaffold overwrites it.

### Commits added to `dev` while away

- `1a054ea docs: phase 2 implementation plan (supabase foundation)`
- `174fc19 docs: phase 3 plan (schema + RLS + Realtime gate)`
- `160470c docs: spec audit fixes from phase 2-3 planning`

Plus `2f5c5bb chore: populate project memory with conventions + pointers` from earlier in session.

### Branches as of end-of-session

- `main` — at `e1c023f` (Phase 1 plan), production-ready
- `dev` — 4 commits ahead of `main` (memory reorg, Phase 2 plan, Phase 3 plan, spec audit fixes)
- `feat/conventions-stage` — 1 commit ahead of `dev` (pre-staged conventions, NOT merged)

### Open / next pickup

When you return:

1. **Skim the Phase 2 + Phase 3 plans** to validate they match your mental model. Either is ~30 min read.
2. **Resume Phase 1 plan at Task 3** (`docs/superpowers/plans/2026-05-02-v2-phase-1-repo-and-ci.md`). That's the snapshot-conventions-backup → run `bun create next-app` flow. Tasks 3-10 take ~30 min; you'll need to be present for `create-next-app` prompts.
3. **After scaffold lands on `feat/scaffold-next`**, merge `feat/conventions-stage` into it to bring vercel.json, CI yaml, PR template, AGENTS.md, .mcp.json, and CLAUDE.md AI rails forward without retyping.
4. **Replace scaffold-default README.md** with the v2 quickstart from Phase 1 plan Task 18.
5. After that, Phase 1 Tasks 11-25 to ship Phase 1.
6. Then move to Phase 2 (Supabase foundation), Phase 3 (gate), and so on.

### Carried-over open threads

- `git config user.email` not yet set on v2 repo. Per `~/.claude/memory/general.md`: use `taylor@redlamp.org`.
- `.mcp.json` needs `project_ref` query param appended after v2 Supabase project creation in Phase 2 Task 7.
- v1 repo `narrative-chess-v1` privatize → wait until v2 M1 ships (decided).
- v1 Supabase project pause → Phase 2 Task 6.
- `~/.claude/cache/memory-loaded-*` flag-file sweep — still open.
