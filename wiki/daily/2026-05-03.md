# 2026-05-03

## Big day — M1 shipped + Phase 7 staged on dev

### Phase 5 ship — board UI + Realtime sync

PR #8 (`6178632`) — board with drag + click-to-move, optimistic `fen` update + ref-tracked rollback for race-safe applyMove, `lib/realtime/subscribe.ts` typed wrapper, observer mode (third+ authenticated viewer with the URL gets read-only), check/checkmate king highlights, sidebar pills with team color framing.

Polish PR #9 (`eaf38e0`) — drag flicker fixed via optimistic-fen update, `applyMove` memoized for the chessboard prop, type extracts.

Closeout PR #10 (`9474e7a`) — followups note + spec post-ship + observer e2e.

**Realtime auth-before-subscribe lesson** — `setAuth` race against `channel.subscribe()` silently denies all `postgres_changes` events. Diagnosed via Supabase MCP `select user_sub from realtime.subscription` returning NULL. Fix: `subscribeToMoves` made async, await getSession + setAuth before `.subscribe()`. Captured at [[lesson-realtime-auth-before-subscribe]].

### Phase 6 ship — game end states

PR #11 (`dd0bdb4`) — `resign` + `abort` RPCs, `TerminalBanner` component, status updates over Realtime, e2e for resign-abort-checkmate.

ESM circular import gotcha during Task 8 — plan said `import { TerminationReasonSchema } from "./game"` in `move.ts`, but `game.ts` imports `GameStatusSchema` from `move.ts`. Resolved by relocating `TerminationReasonSchema` to `move.ts`; `game.ts` re-exports.

### M1 ship — dev → main via PR #12

`e81a3d9` on `main` (squash). Production live at `https://narrative-chess-70w492vd6-taylor-8571s-projects.vercel.app`.

**Squash-merge add/add conflict pattern** — 4 files conflicted (actions.ts, engine.test.ts, engine.ts, move.ts) due to dev/main SHA divergence even though content was identical. Pattern: stash → merge origin/main into dev → `git checkout --ours` for all four → reconciliation merge commit → push → retry squash. Captured at [[lesson-dev-main-merge-after-squash]].

### Phase 7 ship (on dev) — games directory + observer count

PR #13 (`b5bd14d`) — `app/games/page.tsx` directory listing four groups (My active / My open / Other open / My completed), `game_observers` table + `register_observer` RPC + RLS, `<ObserverCount>` pill driven by Supabase Realtime Presence (FNV-1a-hashed `user_id:game_id` presence key to avoid leaking user_ids). Manual two-browser smoke had a cookie-sharing diagnosis hiccup (both browsers signed in as Taylor) — sorted out, confirmed working.

### Phase 7 follow-up bug fix

PR #15 (`1a4dd11`) — resigning a game didn't return to `/games`. Added "Back to games" outline button to `TerminalBanner` next to "Start new game". Squash-merged.

### Phase 8 spec + plan written (not executed)

`feat/phase-8-landing-and-auth-header` carries:
- `docs/superpowers/specs/2026-05-03-v2-phase-8-landing-and-auth-header-design.md`
- `docs/superpowers/plans/2026-05-03-v2-phase-8-landing-and-auth-header.md`

Three.js + R3F + Drei + GSAP for landing hero, top-right login/sign-up modal using Supabase shadcn pattern, two-line "Narrative Chess" title, white logo, dark grey bg dark mode / amber-100 cream light mode. Simple primitives for hero pieces.

### M1.5 scope locked

User picked Phase 7 (games dir + observer count) + Phase 8 (landing + auth header) as the M1.5 cut. Distinct viewer count via `game_observers` table. Modal login uses Supabase's terms.

### Backlog items added

- Dev-only "fool's mate" debug button — drives both clients through `f3 e5 g4 Qh4#` to checkmate state for fast smoke. Gate behind `NODE_ENV !== "production"`.

### Notes created today

- [[lesson-realtime-auth-before-subscribe]]
- [[lesson-dev-main-merge-after-squash]]

### Carried open threads → 2026-05-04

- Phase 7 prod smoke on dev preview, then `dev` → `main` PR
- Phase 8 execution (subagent-driven, plan task 1 first)
- Step N — privatize v1 (`gh repo edit redlamp/narrative-chess-v1 --visibility private --accept-visibility-change-consequences`)
- alt@redlamp.org password reset via Supabase Studio (deferred)
